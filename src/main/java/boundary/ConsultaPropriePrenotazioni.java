package boundary;

import com.intellij.uiDesigner.core.GridConstraints;
import com.intellij.uiDesigner.core.GridLayoutManager;
import com.intellij.uiDesigner.core.Spacer;

import javax.swing.*;
import java.awt.*;
import javax.swing.table.*;
import java.awt.event.*;

public class ConsultaPropriePrenotazioni {
    private JPanel panel;
    private JTable table1;

    {
// GUI initializer generated by IntelliJ IDEA GUI Designer
// >>> IMPORTANT!! <<<
// DO NOT EDIT OR ADD ANY CODE HERE!
        $$$setupUI$$$();
    }

    /**
     * Method generated by IntelliJ IDEA GUI Designer
     * >>> IMPORTANT!! <<<
     * DO NOT edit this method OR call it in your code!
     *
     * @noinspection ALL
     */
    private void $$$setupUI$$$() {
        createUIComponents();
        panel = new JPanel();
        panel.setLayout(new GridLayoutManager(2, 3, new Insets(0, 0, 0, 0), -1, -1));
        final Spacer spacer1 = new Spacer();
        panel.add(spacer1, new GridConstraints(0, 1, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_HORIZONTAL, GridConstraints.SIZEPOLICY_WANT_GROW, 1, null, null, null, 0, false));
        final Spacer spacer2 = new Spacer();
        panel.add(spacer2, new GridConstraints(1, 0, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_VERTICAL, 1, GridConstraints.SIZEPOLICY_WANT_GROW, null, null, null, 0, false));
        final Spacer spacer3 = new Spacer();
        panel.add(spacer3, new GridConstraints(1, 2, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_VERTICAL, 1, GridConstraints.SIZEPOLICY_WANT_GROW, null, null, null, 0, false));
        final JScrollPane scrollPane1 = new JScrollPane();
        panel.add(scrollPane1, new GridConstraints(1, 1, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_BOTH, GridConstraints.SIZEPOLICY_CAN_SHRINK | GridConstraints.SIZEPOLICY_WANT_GROW, GridConstraints.SIZEPOLICY_CAN_SHRINK | GridConstraints.SIZEPOLICY_WANT_GROW, null, null, null, 0, false));
        scrollPane1.setViewportView(table1);
    }

    /**
     * @noinspection ALL
     */
    public JComponent $$$getRootComponent$$$() {
        return panel;
    }

    private void createUIComponents() {
        var colonne = new String[]{"Prenotazione", "Fila", "Posto", "Servizi Aggiuntivi", "Costo", " "};
        var dati = new Object[][]{
                {"Prenotazione 1", "Intermedia", "17", "Parcheggio", "20€", "Annulla"},
                {"Prenotazione 2", "Prima", "7", "Parcheggio, Cabina", "30€", "Annulla"}
        };
        DefaultTableModel model = new DefaultTableModel(dati, colonne);

        table1 = new JTable(model);

        TableColumn colonna = table1.getColumnModel().getColumn(5);
/*
        // creo una classe che gestisce l'ultima colonna "bottone"
        GestoreBottoneAnnulla gestoreBottone = new GestoreBottoneAnnulla(table1, model);
        colonna.setCellRenderer(gestoreBottone);
        colonna.setCellEditor(gestoreBottone);
    }

    private class GestoreBottoneAnnulla extends AbstractCellEditor implements TableCellRenderer, TableCellEditor, ActionListener {

        private final JTable tabella;
        private final DefaultTableModel modello;
        private final JButton bottoneRender;
        private final JButton bottoneEditor;
        private Object valoreCella;

        public GestoreBottoneAnnulla(JTable tabella, DefaultTableModel modello) {
            this.tabella = tabella;
            this.modello = modello;
            this.bottoneRender = new JButton();
            this.bottoneEditor = new JButton();
            this.bottoneEditor.addActionListener(this);
        }


        @Override
        public Component getTableCellRendererComponent(JTable table, Object value, boolean isSelected, boolean hasFocus, int row, int column) {
            bottoneRender.setText((value == null) ? "" : value.toString());
            return bottoneRender;
        }

        @Override
        public Component getTableCellEditorComponent(JTable table, Object value, boolean isSelected, int row, int column) {
            this.valoreCella = value;
            bottoneEditor.setText((value == null) ? "" : value.toString());
            return bottoneEditor;
        }

        @Override
        public Object getCellEditorValue() {
            return this.valoreCella;
        }

        @Override
        public void actionPerformed(ActionEvent e) {
            int rigaSelezionata = tabella.convertRowIndexToModel(tabella.getEditingRow());
            fireEditingStopped();

            int scelta = JOptionPane.showConfirmDialog(panel,
                    "Sei sicuro di voler annullare la prenotazione selezionata?",
                    "Conferma Annullamento",
                    JOptionPane.YES_NO_OPTION);

            if (scelta == JOptionPane.YES_OPTION) {
                modello.removeRow(rigaSelezionata);
                JOptionPane.showMessageDialog(panel, "Prenotazione annullata con successo.");
            }
        }

 */
    }
}



